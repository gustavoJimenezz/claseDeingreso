function mostrar()
{
	var sexo
	var sexonotamax
	var sexonotamin
	var numero=0;

	var contador=0;
	var nummax;
	var nummin;
	// declarar variables
	
	var respuesta='si';

	//while(respuesta!='no')
	do{
		numero=parseInt(prompt("ingresar numero"))
		
		if (contador==0) {
			nummax=numero
			nummin=numero
			
		}
		else{
			if(numero>nummax){
				nummax=numemro
		
	
			}
	
		if(numero<nummin){
		 nummin=numero
			
		
	
			}
	contiuar=prompt("deasea seguir ?")
	contador++	
	}while(continuar=="si")
	document.getElementById("")
	document.getElementById("")




}//FIN DE LA FUNCIÓN